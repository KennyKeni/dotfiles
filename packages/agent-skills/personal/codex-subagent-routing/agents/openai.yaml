interface:
  display_name: "Codex Subagent Routing"
  short_description: "Route Codex work through selected lanes"
  default_prompt: "Use $codex-subagent-routing to delegate this work through the appropriate native Codex or OpenCode execution lane."
policy:
  allow_implicit_invocation: false
