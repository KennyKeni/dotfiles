interface:
  display_name: "Claude OpenCode Grok First"
  short_description: "Delegate Claude work to Grok via OpenCode"
  default_prompt: "Use $claude-opencode-grok-first to delegate this implementation task through OpenCode/Grok."
policy:
  allow_implicit_invocation: false
