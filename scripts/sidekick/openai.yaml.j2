interface:
  display_name: "Sidekick"
  short_description: "Pair the current Codex lead with a persistent sidekick"
  default_prompt: "Use $sidekick to complete this coding task with the current session as lead and a persistent {{ default_setup.title }} sidekick."
policy:
  allow_implicit_invocation: false
